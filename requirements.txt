# FastAPI and server
fastapi==0.115.5
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.11.7
pydantic-settings==2.10.1
gunicorn

# Redis (used for token/session cache)
redis==6.2.0

# JWT auth (shared secret with NestJS) — HS256 only, no crypto extra needed
pyjwt==2.9.0

# AI / LLM
openai>=1.0.0

# Audio — TTS via AWS Polly
boto3==1.35.96

# Utilities
python-dotenv==1.1.1
requests==2.32.4
